# IaC Factory LITE

Versione LITE di **IaC Factory**: un semplice tool da linea di comando che
legge un file YAML e genera un file `main.tf` Terraform di esempio.

Questa versione:

- supporta solo output Terraform (no Ansible)
- genera un singolo file `main.tf`
- è pensata come base open-source / dimostrativa

Per la versione PRO (Terraform + Ansible, template estesi, bundle ZIP, ecc.)
